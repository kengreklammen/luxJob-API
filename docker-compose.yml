services:
  shiny:
    image: rhino-app:latest
    container_name: rhino_app_container
    restart: unless-stopped
    ports:
      - "8008:8008"
    volumes:
      - "./app/:/srv/shiny-server"
      - "./log/shiny-server/:/var/log/shiny-server/"